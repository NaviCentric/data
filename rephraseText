const greenLapCell = this.page.locator(/* your xpath */);
await greenLapCell.scrollIntoViewIfNeeded();
await this.page.waitForTimeout(300); // let Wijmo settle post-scroll
const titleNow = await greenLapCell.getAttribute('title'); // or the ancestor row's title
console.log('title right before click:', titleNow);
await greenLapCell.dblclick();
