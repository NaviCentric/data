One solution which is coming to my mind is: since it's working locally with a manually-started JNLP agent, we could configure auto-logon for the service account on the CI box and launch the agent as a Scheduled Task tied to that logon session (instead of running it as a Windows Service) — so it runs in a real interactive session rather than Session 0
