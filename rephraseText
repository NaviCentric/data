/**
 * Composed fixtures
 * Combines components + data provider + validators for ergonomic test authoring.
 */

import { expect, test as base } from '../../core/infrastructure/index';
import {
  componentsFixturesNoPage,
  componentsFixturesWithPage,
  type ComponentsFixture,
} from './components.fixture';
import {
  dataProviderFixturesNoPage,
  dataProviderFixturesWithPage,
  type DataProviderFixture,
} from './dataprovider.fixture';
import {
  stsFixtures,
  stsMainScreenFixtures,
  stsProjectSelectedFixtures,
  type StsFixture,
  type StsMainScreenFixture,
  type StsProjectSelectedFixture,
} from './sts.fixture';
import { validatorFixtures, type ValidatorFixture } from './validator.fixture';

export type AppComposedFixture = ComponentsFixture & DataProviderFixture & ValidatorFixture;
export type StsComposedFixture = AppComposedFixture & StsFixture;

const appFixtureConfigWithPage = {
  ...componentsFixturesWithPage,
  ...dataProviderFixturesWithPage,
  ...validatorFixtures,
};

const appFixtureConfigNoPage = {
  ...componentsFixturesNoPage,
  ...dataProviderFixturesNoPage,
  ...validatorFixtures,
};

const ves2UiAppTest = base.extend<AppComposedFixture>(appFixtureConfigWithPage);
const ves2NoPageAppTest = base.extend<AppComposedFixture>(appFixtureConfigNoPage);

export const ves2UiTest = ves2UiAppTest;
export const ves2ApiTest = ves2NoPageAppTest;
export const ves2DbTest = ves2NoPageAppTest;
export const ves2Test = ves2UiTest;
export const stsTest = base.extend<StsComposedFixture>({
  ...dataProviderFixturesNoPage,
  ...validatorFixtures,
  ...stsFixtures,
});

export const stsMainScreenTest = stsTest.extend<StsMainScreenFixture>({
  ...stsMainScreenFixtures,
});

export const stsProjectSelectedTest = stsMainScreenTest.extend<StsProjectSelectedFixture>({
  ...stsProjectSelectedFixtures,
});

export { expect };
